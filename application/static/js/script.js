const newsCards=document.querySelector(".news-cards"),searchItem=document.querySelector("#searchItem"),searchBtn=document.querySelector("#searchBtn");function getData(e){fetch(`/search=${e}`).then(e=>e.text()).then(e=>{e.trim().length>0?newsCards.innerHTML=e:alert("Data not found for the given search result.")})}searchBtn.addEventListener("click",e=>{e.preventDefault();let t=String(searchItem.value);t&&t.trim().length>0?getData(t):alert("No data provided to get the News Feed...")});